const config = {
    db: {
        host: "localhost",
        user: "backend",
        password: "backendpassword",
        database: "SpilgStore",
    },
    itemsPerPage: 10,
}

module.exports = config

